# Python
__pycache__/
*.py[cod]
*.pyo
.venv/

# Model weights (large files, download via setup.sh)
models/

# Generated audio output cache
output/

# macOS
.DS_Store
test_results/

# Runtime
server.log
server.pid
lift_me_higher.wav
*.log
*.pid

# Mastering
data/reference.wav

# Preset (user-specific, not committed)
# preset.json is intentionally NOT gitignored — commit your preset for reproducibility

# Logs (rotated files committed only if needed)
logs/*.gz
logs/*.log
