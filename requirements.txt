# Core TTS (XTTS-v2)
# `coqui-tts` is the maintained fork of Coqui `TTS` (same `TTS` import namespace).
# The original `TTS` package is pinned to Python <3.12 and no longer installs on
# Ubuntu 24.04 (Python 3.12).  The fork ships its own transformers compatibility,
# so the old stream_generator / gpt_inference patches are no longer needed.
coqui-tts[ja]>=0.27.0,<0.28   # [ja] = cutlet + mecab for Japanese text
pypinyin>=0.50                  # required for zh-cn
spacy[ja]>=3.7,<4               # sentence splitting for texts >=250 chars (enable_text_splitting)

# coqui-tts 0.27.x targets transformers 4.57+; 5.x has not been validated with it.
transformers>=4.57,<5

# Music generation — ACE-Step 1.5 (installed from GitHub by setup.sh, opt-in)
# pip install git+https://github.com/ace-step/ACE-Step-1.5.git

# HuggingFace model download — bypasses the broken Coqui/scarf.sh gateway
huggingface_hub>=0.20.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-multipart>=0.0.9   # needed by POST /voices and POST /preset/reference (file upload)

# Audio mastering post-processing
pedalboard>=0.9.0
matchering>=2.0.6
pyloudnorm>=0.1.1

# torch + torchaudio installed by setup.sh with correct CUDA/CPU variant.
# Pinned to 2.5.x — torchaudio 2.6 switched to torchcodec which breaks TTS.
# Do NOT add them here.
