# Core TTS (XTTS-v2)
TTS>=0.22.0

# transformers 4.46+ removed BeamSearchScorer which TTS/XTTS depends on
transformers>=4.33.0,<4.46.0

# Music generation — ACE-Step 1.5 (installed from GitHub by setup.sh)
# pip install git+https://github.com/ace-step/ACE-Step-1.5.git

# HuggingFace model download — bypasses the broken Coqui/scarf.sh gateway
huggingface_hub>=0.20.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# torch + torchaudio installed by setup.sh with correct CUDA/CPU variant.
# Pinned to 2.5.x — torchaudio 2.6 switched to torchcodec which breaks TTS.
# Do NOT add them here.
